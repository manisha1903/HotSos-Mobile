import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'hotsos-mobile',
  webDir: 'dist/hotsos-mobile/browser'
};

export default config;
